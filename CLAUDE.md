# CodeY CMS

Follow `AGENTS.md` and the canonical contract in `docs/generated-site-contract.md`.

Use the runtime's authenticated generation-contract endpoint before creating WebsiteSpec content. Do not invent capabilities or bypass validation, permissions, migrations, signed releases, or browser acceptance.
